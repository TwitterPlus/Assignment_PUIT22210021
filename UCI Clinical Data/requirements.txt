# Requirements for the `Heart Failure.ipynb` notebook
# These packages are required to run the data loading, analysis, visualization,
# and machine learning cells in `Heart Failure.ipynb`.
# Install with: `pip install -r requirements.txt`

pandas>=1.3
numpy>=1.21
matplotlib>=3.4
seaborn>=0.11
scikit-learn>=1.0
scipy>=1.7
jupyter>=1.0
notebook>=6.4
ipykernel>=6.0

# Notes:
# - `pandas`, `numpy`, `matplotlib`, and `seaborn` are used for data handling and plots.
# - `scikit-learn` provides the modeling and evaluation functions used in the notebook.
# - `jupyter` / `notebook` / `ipykernel` are provided for running the notebook environment.
# - Adjust versions if you need to match an existing environment.